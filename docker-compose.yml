services:
  tempmail:
    build: .
    container_name: tempmail
    restart: unless-stopped
    env_file:
      - .env
    ports:
      # 生产环境可设置 WEB_BIND_HOST=127.0.0.1，避免绕过 HTTPS 反向代理直接访问管理页面。
      - "${WEB_BIND_HOST:-0.0.0.0}:8081:5000"
      - "25:25"       # SMTP 需保持公网收信能力
    volumes:
      - ./data:/app/data
